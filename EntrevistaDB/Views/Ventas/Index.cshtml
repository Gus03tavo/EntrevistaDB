@model IEnumerable<EntrevistaDB.Models.Venta>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VentaFecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VentaImporteTotal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClDniNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VentaFecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VentaImporteTotal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClDniNavigation.ClDni)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VentaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VentaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VentaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
