@model EntrevistaDB.Models.Venta

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Venta</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ClDni" class="control-label"></label>
                <select asp-for="ClDni" class ="form-control" asp-items="ViewBag.ClDni"></select>
            </div>
            <div class="form-group">
                <label asp-for="VentaFecha" class="control-label"></label>
                <input asp-for="VentaFecha" class="form-control" type="date"/>
                <span asp-validation-for="VentaFecha" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="VentaImporteTotal" class="control-label"></label>
                <input asp-for="VentaImporteTotal" class="form-control" />
                <span asp-validation-for="VentaImporteTotal" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
